{
    "version": "2.0.0",
    "type": "shell",
    "tasks": [
        {
            "label": "build",
            "command": "source ~/.zshrc && colcon build --symlink-install && find . -wholename './bot_one/src/*.py' -exec chmod +x {} \\;", 
    ]   
}